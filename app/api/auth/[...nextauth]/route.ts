/**
 * NextAuth.js v5 핸들러 — Google OAuth callback.
 * /api/auth/signin, /api/auth/callback/google, /api/auth/signout 등 자동.
 */
import { handlers } from "@/auth";
export const { GET, POST } = handlers;
